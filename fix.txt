Usuário
Adicionar idUnidade

Animal 
idAnimal fica
dataEntrada fica
nomeAgente sai
idEntregador fica
idUsuario fica
idUnidade saí e entra no Usuário
destinacao

Criar nova tabela -> DadosTriagem
codMarcacao
tipoMarcacao
localMarcacao
ultimaProcedencia
nomeComum
nomeCientifico
familia
ordem


Tabela Quarentena 
idAnimal
periodoQuarentena

Tabela Destinação
modalidadeDestinacao 
subtipoModalidade
idArea

condicao1 transformar em javascript
condicao2
condicao3

dataCriacao
dataModificacao


Relatório



Chaves estrangeiras
Animal(idEntregador, usuario, triagem, quarentena, destincao)
Area(idProprietario, idEndereco)
entregadores(idAnexo, idEndereco)
unidades(idEndereco)
usuarios(idUnidade)
quarentena(idAnimal)
destinacao(idArea)
dadosTriagem(idAnimal)


ALTER TABLE `animais` ADD `idTriagem` INT AFTER `idAnimal`, ADD FOREIGN KEY (`idTriagem`) REFERENCES dadostriagem(`idTriagem`);

ALTER TABLE `animais` ADD `idUsuario` INT AFTER `idAnimal`, ADD FOREIGN KEY (`idUsuario`) REFERENCES usuarios(`idUsuario`);


ALTER TABLE `animais` ADD `idQuarentena` INT AFTER `idAnimal`, ADD FOREIGN KEY (`idQuarentena`) REFERENCES dadosquarentena(`idQuarentena`);

ALTER TABLE `animais` ADD `idDestinacao` INT AFTER `idAnimal`, ADD FOREIGN KEY (`idDestinacao`) REFERENCES dadosdestinacao(`idDestinacao`);
